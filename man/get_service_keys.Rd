% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object_methods.R
\name{get_service_keys}
\alias{get_service_keys}
\title{Extracts Available Service Keys from a ChatRBox Services List}
\usage{
get_service_keys(services)
}
\arguments{
\item{services}{List. A ChatRBox services list, with a \code{tools} environment and an \code{api_services} named list, as constructed in \code{\link{object_generate}}. Required.}
}
\value{
A character vector of unique service keys (tool names and API service names). Returns \code{character(0)} when no services are present.
}
\description{
This function returns the user-facing service keys present in a ChatRBox services list, namely every tool function name and every API service name. These keys correspond to the names a user supplies in \code{tools_list}, \code{services_list} and \code{openapi_list}, and are the keys used to match \code{summary_list} entries (and tokens/headers) to a given service.
}
\details{
This function is used to validate \code{summary_list} names against the available services in \code{\link{object_generate}} and \code{\link{property_generate}}, reusing the same checks applied to API tokens and headers (\code{\link{check_names}}, \code{\link{validate_name_match}}, \code{\link{check_duplicate_names}}). This facilitates the name-matching logic underpinning the ChatRBox workflow, whereby given summary prompts and token information are matched to the appropriate service based on user-supplied naming during chatbot initialization. Hence, errors will be thrown for incorrect or duplicated user naming conventions.
}
\examples{
# Defines tool function
add <- function(x,y) {
  x + y
}

# Generates S7 chatbot object with tool function
object <- object_generate(tools_list = list(add_two_numbers = add))

# Extracts tool environment from S7 object
tools <- get_property(object, "tools_env")

# Extracts services list from S7 object
services <- get_property(object, "services")

# These both contains the 'add' function under the name 'add_two_numbers'
ls(tools)
get_service_keys(services)

# This converts contents of tools into list for AI prompt
env_to_str(tools)

# Example AI response for addition of two numbers
response <- 'This service adds two numbers together!```json
{
 "path": "services$tools$add_two_numbers",
 "name": "add",
 "x": 70,
 "y": 76
}
```'
# Populates 'add' client function with extracted arguments from user question
result <- llm_api_result(llm_response = response,
                         object = object)

# Outputs tool result without accompanying AI dialogue
result

# Defines second tool function
subtract <- function(x,y) {
  x - y
}

# Outputs list of updated S7 properties given second tool function and 
# existing S7 tools environment
# Non-referenced arguments take default values
property_generate(object = object,
                  tools_env = object@tools_env,
                  tools_list = list(subtract_two_numbers = subtract))


# property_generate() is used as a helper function to update the S7 
# chatbot object in-place
# Here, the existing S7 tools environment is a default
update <- object_update(object = object,
                        tools_list = list(subtract_two_numbers = subtract))

# Extracts tool environment from updated S7 object
updated_tools <- get_property(update, "tools_env")

# This contains the 'add' and 'subtract' functions
ls(updated_tools)

# This converts contents of updated_tools into list for AI prompt
env_to_str(updated_tools)
}
\seealso{
\code{\link{process_summary_list}}, \code{\link{object_generate}}, \code{\link{check_names}}, \code{\link{validate_name_match}}, \code{\link{check_duplicate_names}}
}
